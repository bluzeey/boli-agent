"$schema" = "https://railway.app/railway.schema.json"

[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = ["echo '[deploy] running alembic upgrade head' && alembic upgrade head 2>&1"]
startCommand = "echo '[deploy] starting gunicorn on port ${PORT:-8000}' && gunicorn app.main:app --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 600 --access-logfile - --error-logfile - --log-level info -k uvicorn.workers.UvicornWorker"
healthcheckPath = "/health"
healthcheckTimeout = 120
healthcheckInterval = 15
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
